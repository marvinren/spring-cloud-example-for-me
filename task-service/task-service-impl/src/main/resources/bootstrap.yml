spring:
  application:
    name: task-service
  cloud:
    config:
      fail-fast: true
      uri:  http://localhost:8761
      profile: dev
  profiles:
    active: #spring.profiles.active#

info:
    project:
        version: #project.version#
